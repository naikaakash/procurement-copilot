# 📚 Prompt Library

*Stored prompts that produce premium architecture and UI layout.*

### 🛠️ Prompt: Build Premium Enterprise Dashboard Table
> *"Build an enterprise supply chain table featuring sticky headers, custom HSL status badges, alternate row colors, hover states, fixed columns widths, and an entries selector dropdown. Ensure the layout supports native full-page browser scrolling and keeps the side drawer fixed to the viewport."*

### 🛠️ Prompt: Decouple Service from Storage
> *"Create a data service layer that parses and joins CSV files in memory. Expose functions that return clean typed interfaces. Do not expose CSV-reading logic to the API route handlers, so we can transition to a SQL database later by only changing this service file."*
