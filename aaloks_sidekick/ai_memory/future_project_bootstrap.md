# 🗺️ Future Project Bootstrap Blueprint

*This file outlines how future AI agents should initialize any new project matching Aalok's style.*

### 🛠️ Inception Directives
1. **Initialize service layer first**: Do not build UI mock elements. Start with a data parser/connector.
2. **Build with high-end dark HSL colors**: Set CSS tokens for dark slates immediately in `globals.css`.
3. **Configure strict validation**: Create the QA checklist inside `project_docs/` before coding.
